name: .NET Build & Test

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Check out repo
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Setup .NET
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      # 3️⃣ Restore dependencies
      - name: Restore dependencies
        run: dotnet restore FanDuelDepthChart.sln

      # 4️⃣ Build the solution
      - name: Build
        run: dotnet build FanDuelDepthChart.sln --no-restore --configuration Release

      # 5️⃣ Run tests
      - name: Run tests
        run: dotnet test FanDuelDepthChart.sln --no-build --verbosity normal --configuration Release
